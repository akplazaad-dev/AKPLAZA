# 📘 카드 사은혜택 자동 입력 프로그램 — 사용설명서 (윈도우용)

> **이 프로그램이 하는 일**
> 이번 달 **제휴 판촉 캘린더 PDF**를 넣고 실행하면,
> **엑셀 양식의 노란색 칸(주차별 카드 사은혜택 칸)**을 자동으로 채워
> **새 엑셀 파일**을 만들어 줍니다.
>
> 컴퓨터를 처음 다루는 분도 따라 할 수 있도록, 아주 자세히 순서대로 적었습니다.
> **처음 한 번만** [1단계]~[3단계]를 하고, 그다음부터는 매달 [4단계]만 반복하면 됩니다.

---

## ✅ 먼저 알아두기 (딱 3가지)

1. **처음 딱 한 번**만 프로그램(파이썬)을 설치합니다. → [1단계], [2단계], [3단계]
2. **매달 반복**하는 일은 아주 간단합니다. → [4단계] (파일 2개 넣고 더블클릭)
3. 프로그램은 **원본을 절대 건드리지 않고**, 항상 **새 파일**을 만들어 줍니다. 안심하세요.

---

## 🗂️ 준비물 (폴더 안에 들어 있어야 하는 것)

이 프로그램 폴더 안에는 아래 파일들이 있습니다. **지우지 마세요.**

| 파일 이름 | 설명 |
|---|---|
| `update_calendar.py` | 실제 프로그램 (내용은 몰라도 됩니다) |
| `최초설치.bat` | 처음 한 번만 실행하는 설치 파일 |
| `실행하기.bat` | 매달 더블클릭해서 실행하는 파일 |
| `requirements.txt` | 설치에 필요한 목록 (건드리지 마세요) |
| `사전고지_양식.xlsx` | **엑셀 양식** (노란색 칸이 있는 파일) |
| `예제/` 폴더 | 연습용 예제 PDF가 들어 있습니다 |
| `사용설명서.md` | 지금 보고 있는 이 파일 |

---

## 1단계: 파이썬(Python) 설치 — 처음 한 번만

파이썬은 이 프로그램을 움직이는 무료 프로그램입니다.

1. 인터넷 브라우저(엣지, 크롬 등)를 열고 아래 주소로 갑니다.
   👉 **https://www.python.org/downloads/**
2. 노란색/파란색 큰 버튼 **`Download Python 3.x.x`** 를 눌러 설치 파일을 내려받습니다.
3. 내려받은 파일(예: `python-3.xx.x-amd64.exe`)을 **더블클릭**합니다.
4. ⚠️ **아주 중요!** 설치 첫 화면 맨 아래에 있는
   ☑️ **`Add python.exe to PATH`** (또는 `Add Python to PATH`) 체크박스를 **꼭 켜세요.**
   (이걸 안 켜면 프로그램이 파이썬을 못 찾습니다.)
5. 그다음 **`Install Now`** 를 눌러 설치를 끝냅니다.
6. 설치가 끝나면 **`Close`** 를 눌러 창을 닫습니다.

> 💡 이미 파이썬이 깔려 있는 회사 PC라면 이 단계는 건너뛰어도 됩니다.
> 잘 모르겠으면 그냥 위 순서대로 설치해도 문제 없습니다.

---

## 2단계: 프로그램 폴더를 원하는 위치에 두기 — 처음 한 번만

1. 이 프로그램 폴더(파일들이 들어 있는 폴더)를 통째로
   **바탕화면**이나 **문서** 폴더처럼 찾기 쉬운 곳에 둡니다.
2. 폴더 이름/위치는 자유롭게 정해도 됩니다.

> ⚠️ 폴더 경로(위치)에 한글이 있어도 괜찮습니다. 그냥 편한 곳에 두세요.

---

## 3단계: 최초 설치 실행 — 처음 한 번만

1. 폴더 안의 **`최초설치.bat`** 파일을 **더블클릭**합니다.
2. 검은색 창이 뜨고 글자가 주르륵 지나갑니다. (부품을 내려받는 중입니다.)
3. 마지막에 **`설치 완료!`** 라는 글자가 보이면 성공입니다.
4. 아무 키나 누르면 창이 닫힙니다.

> 🔒 **윈도우가 "보안 경고" 창을 띄우는 경우**
> "Windows의 PC 보호" 같은 파란 창이 뜨면
> → **`추가 정보`** 클릭 → **`실행`** 버튼을 누르면 됩니다.
> (회사에서 만든 내부용 파일이라 그렇습니다.)

---

## 4단계: 매달 사용하기 — 이것만 반복하면 됩니다! ⭐

매달 새 PDF가 나오면 아래 3가지만 하면 끝입니다.

### ① 이번 달 PDF를 폴더에 넣기
- 이번 달 **제휴 판촉 캘린더 PDF 파일**을 이 프로그램 폴더 안에 **복사해 넣습니다.**
- (지난달 PDF가 남아 있으면 지우거나 밖으로 빼는 것을 권장합니다. 프로그램은 **가장 최근에 넣은 PDF**를 사용합니다.)

### ② 엑셀 양식이 폴더에 있는지 확인
- `사전고지_양식.xlsx` 파일이 폴더에 있어야 합니다. (보통 계속 그대로 두면 됩니다.)
- 양식이 바뀌면 새 양식으로 교체해 넣으세요. 파일 이름에 **"결과"** 라는 글자만 안 들어가면 됩니다.

### ③ `실행하기.bat` 더블클릭
- **`실행하기.bat`** 을 **더블클릭**합니다.
- 검은 창에 아래처럼 나오면 성공입니다.

  ```
  [찾은 월간 공통 혜택] 총 8건
     ✓ 신한Plus 발급①
     ✓ ...
  [완료] 새 엑셀 파일을 만들었습니다:
     → 사전고지_양식_2026년10월_결과.xlsx
  ```

- 폴더 안에 아래 두 파일이 새로 생깁니다.
  - 📗 `..._결과.xlsx` → **노란색 칸이 채워진 완성 엑셀 파일**
  - 📄 `..._검토용.txt` → 사람이 눈으로 확인하는 참고용 정리 파일

---

## 5단계: 결과 확인하기 (아주 중요!) 🔍

1. 새로 생긴 **`..._결과.xlsx`** 파일을 엽니다.
2. `사전고지(본부)` 시트의 **노란색 칸(주차별 지원 사항)**에 사은혜택이 들어갔는지 봅니다.
3. **`..._검토용.txt`** 파일도 열어 봅니다.
   - **[1] 부분**: 엑셀 칸에 실제로 들어간 "월간 공통 혜택" 목록입니다.
   - **[2] 부분**: PDF에 있던 **특정 날짜 행사**(예: 공판·개점행사 등) 후보 목록입니다.
     이 부분은 PDF 디자인 특성상 자동으로 칸에 넣지 않았습니다.
     필요하면 원본 PDF와 비교해 확인한 뒤 **엑셀 칸에 직접 추가**하세요.

> ### ❓ 왜 "월간 공통 혜택"만 자동으로 채워지나요?
> 이 판촉 캘린더 PDF는 디자인이 매우 촘촘해서, **한 달 내내 유효한 혜택**
> (신한Plus 발급/정기, PAYCO 포인트, 가전, 가구)은 글자가 깨끗하게 읽혀
> **정확히 자동 입력**됩니다. 이 혜택들은 모든 주차에 공통으로 유효하므로
> 5개 주차 칸에 모두 들어갑니다.
>
> 반면 **특정 주에만 하는 행사**(A*CLASS, 멤버스페스티벌, 공판, 개점행사 등)는
> PDF에서 글자가 날짜 칸에 겹쳐 나와 자동으로 넣으면 틀릴 수 있어서,
> **검토용 파일에 따로 정리**해 드립니다. (사람이 확인 후 직접 추가)

---

## 🛠️ 자주 생기는 문제 해결

### 🔴 "파이썬(Python)이 설치되어 있지 않습니다" 라고 나와요
- [1단계]에서 **`Add Python to PATH` 체크를 안 켰을** 가능성이 큽니다.
- 파이썬을 **삭제 후 다시 설치**하면서 그 체크박스를 꼭 켜 주세요.
- 다시 설치한 뒤 컴퓨터를 한 번 **재시작**하면 확실합니다.

### 🔴 "엑셀 양식 파일(.xlsx)을 찾지 못했습니다"
- 폴더 안에 `사전고지_양식.xlsx` 같은 엑셀 파일이 있는지 확인하세요.
- 파일 이름에 **"결과"** 라는 글자가 들어간 파일은 양식으로 쓰지 않습니다.

### 🔴 "PDF 파일(.pdf)을 찾지 못했습니다"
- 이번 달 PDF를 폴더 안에 **복사해 넣었는지** 확인하세요.

### 🔴 검은 창이 순식간에 나타났다 사라져요
- 이 경우 **거의 대부분 파이썬이 아직 설치되지 않았거나, 설치할 때
  `Add Python to PATH` 체크를 안 켠 상태**입니다.
- 해결: [1단계]로 돌아가 파이썬을 **다시 설치**하면서 그 체크박스를 꼭 켜고,
  설치 후 컴퓨터를 **재시작**한 뒤 다시 시도하세요.
- 그래도 창이 바로 닫힌다면, 아래 "명령창에서 직접 실행" 방법으로
  오류 메시지를 확인할 수 있습니다.

### 🔎 (확실한 확인법) 명령창에서 직접 실행해 오류 메시지 보기
1. 프로그램 폴더를 파일 탐색기로 엽니다.
2. 폴더의 **주소창**(위쪽에 폴더 경로가 보이는 칸)을 마우스로 클릭한 뒤,
   글자를 모두 지우고 `cmd` 라고 입력하고 **Enter**를 누릅니다.
   → 그 폴더 위치에서 검은 명령창이 열립니다.
3. 명령창에 아래를 입력하고 Enter:
   ```
   py -3 --version
   ```
   - 버전 숫자(예: `Python 3.12.1`)가 나오면 파이썬은 정상입니다.
   - `'py'은(는) ... 인식할 수 없는 ...` 이라고 나오면 **파이썬 미설치/PATH 문제**입니다. → [1단계] 다시.
4. 파이썬이 정상이라면, 명령창에 아래를 입력하고 Enter:
   ```
   py -3 update_calendar.py
   ```
   → 이렇게 하면 창이 닫히지 않으므로, 어떤 오류가 나는지 **전체 메시지를 눈으로 볼 수 있습니다.**
   그 메시지를 캡처해 담당자에게 보여 주면 빠르게 해결됩니다.

### 🔴 혜택 건수가 8건보다 적게 나와요
- PDF 양식이 평소와 조금 다를 수 있습니다.
- **`..._검토용.txt`** 파일을 열어 어떤 항목이 빠졌는지 확인하고,
  필요하면 엑셀 칸에 직접 보완하세요.
- 매달 PDF 양식이 크게 바뀌면, 이 프로그램을 만든 담당(개발자)에게
  새 PDF를 전달해 프로그램을 조정하면 됩니다.

---

## 🧪 연습해 보기 (선택)

실제 업무 전에 연습하고 싶다면:

1. `예제` 폴더 안의 예제 PDF를 프로그램 폴더(맨 위)로 **복사**해 넣습니다.
2. `실행하기.bat`을 더블클릭합니다.
3. 결과 엑셀/검토용 파일이 잘 생기는지 확인해 보세요.
4. 연습이 끝나면 생성된 `..._결과.xlsx`, `..._검토용.txt`, 예제 PDF는 지워도 됩니다.

---

문제가 계속되면, 검은 창에 나온 **오류 메시지 전체를 캡처**해서
담당자에게 보여 주면 훨씬 빨리 해결할 수 있습니다. 🙂
